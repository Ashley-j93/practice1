a) clone a git repository and add a new text file.

b) create a separate 'test' branch.

c) edit the same file you created on the 'test' branch and commit that change.

d) check out the master branch, edit the file there as well and commit that change.

e) finally merge the 'test' branch back into the master branch, resolve any conflicts, and commit the final version.